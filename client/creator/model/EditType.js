export default class EditType extends phet.phetCore.EnumerationValue {
  static METADATA = new EditType();
  static COMPONENT = new EditType();
  static CUSTOM_CODE = new EditType();
  static CONNECT_TO_ARRAY = new EditType();

  static enumeration = new phet.phetCore.Enumeration( EditType );
}